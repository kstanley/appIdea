var expect = require('chai').expect;

describe('Mocha', function () {
	// test spec unit test
	it('should run our tests using npm', function () {
		expect(true).to.be.ok;
	});
});


// to.be.ok checks for truthy statments
//
